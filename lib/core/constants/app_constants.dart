class AppConstants {
  // Base API URL
  static const String baseApiUrl = '';

  // Sync interval (in minutes)
  static const int syncIntervalMinutes = 15;

  // Logger settings
  static const String logTag = 'TaskManagerApp';
}
